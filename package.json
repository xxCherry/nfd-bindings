{
  "name": "nativefiledialog",
  "version": "1.0.1",
  "main": "index.ts",
  "scripts": {
    "install": "cmake -B build && cmake --build build --config Release"
  },
  "devDependencies": {
    "@types/node": "^24.7.2"
  }
}
